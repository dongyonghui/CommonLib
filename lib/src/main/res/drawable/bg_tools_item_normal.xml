<?xml version="1.0" encoding="utf-8"?><!-- 自定义输入框背景 -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 填充颜色 -->
    <solid android:color="@color/bg_toolsItemNormalBgColor" />
    <!-- 矩形的圆角半径 -->
    <corners android:radius="@dimen/general_cornerRadius" />
    <!-- 线的宽度，颜色 -->
    <stroke
        android:width="@dimen/dividing_line"
        android:color="@color/bg_toolsListItemLineColor" />
</shape>